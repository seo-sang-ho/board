spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/projectNum1
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true  # SQL ?? ?? ???
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true  # SQL ?? ???

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
